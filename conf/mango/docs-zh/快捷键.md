---
title: 键位绑定 (Key Bindings)
description: 定义键盘快捷键和模式。
---

## 语法

键位绑定遵循以下格式：

```ini
bind[flags]=MODIFIERS,KEY,COMMAND,PARAMETERS
```

- **修饰键**：`SUPER`、`CTRL`、`ALT`、`SHIFT`、`NONE`（使用 `+` 组合，例如 `SUPER+CTRL+ALT`）。
- **按键**：键名（来自 `xev` 或 `wev`）或 keycode（例如 `code:24` 表示 `q`）。

> **信息：** `bind` 会自动将 keysym 转换为 keycode 进行比较。这使其兼容所有键盘布局，但匹配可能并不总是精确。如果某个键组合在你的键盘布局上不起作用，请使用 keycode 代替（例如 `code:24` 而不是 `q`）。

### 标志

- `l`：即使屏幕锁定时也有效。
- `s`：使用 keysym 而不是 keycode 进行绑定。
- `r`：在按键释放时触发而不是按下时。
- `p`：将按键事件传递给客户端。

**示例：**

```ini
bind=SUPER,Q,killclient
bindl=SUPER,L,spawn,swaylock

# 使用 keycode 而不是键名
bind=ALT,code:24,killclient

# 组合修饰键和按键的 keycode
bind=code:64,code:24,killclient
bind=code:64+code:133,code:24,killclient

# 无修饰键绑定
bind=NONE,XF86MonBrightnessUp,spawn,brightnessctl set +5%

# 绑定修饰键本身作为触发键
bind=alt,shift_l,switch_keyboard_layout
```

## 键位模式（子映射）

你可以将键位绑定划分为命名模式。规则：

1. 在一组 `bind` 行之前设置 `keymode=<name>` —— 这些绑定仅在该模式下生效。
2. 如果在绑定之前未设置 `keymode`，则它属于 `default` 模式。
3. 特殊的 `common` 键位模式将其绑定**应用于所有模式**。

使用 `setkeymode` 切换模式，使用 `mmsg -b` 查询当前模式。

```ini
# 'common' 中的绑定在每个模式下都生效
keymode=common
bind=SUPER,r,reload_config

# 默认模式绑定
keymode=default
bind=ALT,Return,spawn,foot
bind=SUPER,F,setkeymode,resize

# 'resize' 模式绑定
keymode=resize
bind=NONE,Left,resizewin,-10,0
bind=NONE,Right,resizewin,+10,0
bind=NONE,Escape,setkeymode,default
```

### 单修饰键绑定

当绑定修饰键本身时，按下时使用 `NONE`，释放时使用修饰键名称：

```ini
# 在 Super 键按下时触发
bind=none,Super_L,spawn,rofi -show run

# 在 Super 键释放时触发
bindr=Super,Super_L,spawn,rofi -show run
```

## 调度器列表

### 窗口管理

| 命令                      | 参数              | 描述                                                         |
| :------------------------ | :---------------- | :----------------------------------------------------------- |
| `killclient`              | -                 | 关闭聚焦的窗口。                                             |
| `togglefloating`          | -                 | 切换浮动状态。                                               |
| `togglefullscreen`        | -                 | 切换全屏。                                                   |
| `togglefakefullscreen`    | -                 | 切换"伪"全屏（保持受限）。                                   |
| `togglemaximizescreen`    | -                 | 最大化窗口（保留装饰/状态栏）。                              |
| `toggleglobal`            | -                 | 将窗口固定到所有标签。                                       |
| `toggle_render_border`    | -                 | 切换边框渲染。                                               |
| `centerwin`               | -                 | 居中浮动窗口。                                               |
| `minimized`               | -                 | 将窗口最小化到 scratchpad。                                  |
| `restore_minimized`       | -                 | 从 scratchpad 恢复窗口。                                     |
| `toggle_scratchpad`       | -                 | 切换 scratchpad。                                            |
| `toggle_named_scratchpad` | `appid,title,cmd` | 切换命名 scratchpad。如果未运行则启动应用，否则显示/隐藏它。 |

### 焦点和移动

| 命令                    | 参数                 | 描述                     |
| :---------------------- | :------------------- | :----------------------- |
| `focusdir`              | `left/right/up/down` | 聚焦方向上的窗口。       |
| `focusstack`            | `next/prev`          | 在栈内循环焦点。         |
| `focuslast`             | -                    | 聚焦上一个活动窗口。     |
| `exchange_client`       | `left/right/up/down` | 与方向上的邻居交换窗口。 |
| `exchange_stack_client` | `next/prev`          | 交换窗口在栈中的位置。   |
| `zoom`                  | -                    | 交换聚焦窗口与 Master。  |

### 标签和显示器

| 命令                      | 参数                                        | 描述                                                                                                                               |
| :------------------------ | :------------------------------------------ | :--------------------------------------------------------------------------------------------------------------------------------- |
| `view`                    | `-1/0/1-9` 或 `mask [,synctag]`             | 查看标签。`-1` = 上一个标签集，`0` = 所有标签，`1-9` = 特定标签，掩码例如 `1\|3\|5`。可选 `synctag`（0/1）将操作同步到所有显示器。 |
| `viewtoleft`              | `[synctag]`                                 | 查看上一个标签。可选 `synctag`（0/1）同步到所有显示器。                                                                            |
| `viewtoright`             | `[synctag]`                                 | 查看下一个标签。可选 `synctag`（0/1）同步到所有显示器。                                                                            |
| `viewtoleft_have_client`  | `[synctag]`                                 | 查看左侧标签并在有客户端时聚焦。可选 `synctag`（0/1）。                                                                            |
| `viewtoright_have_client` | `[synctag]`                                 | 查看右侧标签并在有客户端时聚焦。可选 `synctag`（0/1）。                                                                            |
| `viewcrossmon`            | `tag,monitor_spec`                          | 在指定显示器上查看指定标签。                                                                                                       |
| `tag`                     | `1-9 [,synctag]`                            | 将窗口移动到标签。可选 `synctag`（0/1）同步到所有显示器。                                                                          |
| `tagsilent`               | `1-9`                                       | 将窗口移动到标签而不聚焦它。                                                                                                       |
| `tagtoleft`               | `[synctag]`                                 | 将窗口移动到左侧标签。可选 `synctag`（0/1）。                                                                                      |
| `tagtoright`              | `[synctag]`                                 | 将窗口移动到右侧标签。可选 `synctag`（0/1）。                                                                                      |
| `tagcrossmon`             | `tag,monitor_spec`                          | 将窗口移动到指定显示器上的指定标签。                                                                                               |
| `toggletag`               | `0-9`                                       | 在窗口上切换标签（0 表示所有标签）。                                                                                               |
| `toggleview`              | `1-9`                                       | 切换标签视图。                                                                                                                     |
| `comboview`               | `1-9`                                       | 同时按下查看多个标签。                                                                                                             |
| `focusmon`                | `left/right/up/down/monitor_spec`           | 通过方向或 [monitor spec](/docs/configuration/monitors#monitor-spec-format) 聚焦显示器。                                           |
| `tagmon`                  | `left/right/up/down/monitor_spec,[keeptag]` | 通过方向或 [monitor spec](/docs/configuration/monitors#monitor-spec-format) 将窗口移动到显示器。`keeptag` 为 0 或 1。              |

### 布局

| 命令                       | 参数                 | 描述                                    |
| :------------------------- | :------------------- | :-------------------------------------- |
| `setlayout`                | `name`               | 切换到布局（例如 `scroller`、`tile`）。 |
| `switch_layout`            | -                    | 循环遍历可用布局。                      |
| `incnmaster`               | `+1/-1`              | 增加/减少 master 窗口数量。             |
| `setmfact`                 | `+0.05`              | 增加/减少 master 区域大小。             |
| `set_proportion`           | `float`              | 设置 scroller 窗口比例（0.0–1.0）。     |
| `switch_proportion_preset` | -                    | 循环 scroller 窗口比例预设。            |
| `scroller_stack`           | `left/right/up/down` | 通过方向在 scroller 栈内/外移动窗口。   |
| `incgaps`                  | `+/-value`           | 调整间隙大小。                          |
| `togglegaps`               | -                    | 切换间隙。                              |

### 系统

| 命令                         | 参数            | 描述                                                                                    |
| :--------------------------- | :-------------- | :-------------------------------------------------------------------------------------- |
| `spawn`                      | `cmd`           | 执行命令。                                                                              |
| `spawn_shell`                | `cmd`           | 执行 shell 命令（支持管道 `\|`）。                                                      |
| `spawn_on_empty`             | `cmd,tagnumber` | 在空标签上打开命令。                                                                    |
| `reload_config`              | -               | 热重载配置。                                                                            |
| `quit`                       | -               | 退出 mangowm。                                                                          |
| `toggleoverview`             | -               | 切换概览模式。                                                                          |
| `create_virtual_output`      | -               | 创建无头显示器（用于 VNC/Sunshine）。                                                   |
| `destroy_all_virtual_output` | -               | 销毁所有虚拟显示器。                                                                    |
| `toggleoverlay`              | -               | 切换聚焦窗口的覆盖状态。                                                                |
| `toggle_trackpad_enable`     | -               | 切换触摸板启用。                                                                        |
| `setkeymode`                 | `mode`          | 设置 keymode。                                                                          |
| `switch_keyboard_layout`     | `[index]`       | 切换键盘布局。可选索引（0, 1, 2...）切换到特定布局。                                    |
| `setoption`                  | `key,value`     | 临时设置配置选项。                                                                      |
| `disable_monitor`            | `monitor_spec`  | 关闭显示器。接受 [monitor spec](/docs/configuration/monitors#monitor-spec-format)。     |
| `enable_monitor`             | `monitor_spec`  | 开启显示器。接受 [monitor spec](/docs/configuration/monitors#monitor-spec-format)。     |
| `toggle_monitor`             | `monitor_spec`  | 切换显示器电源。接受 [monitor spec](/docs/configuration/monitors#monitor-spec-format)。 |

### 媒体控制

> **警告：** 某些键盘不发送标准媒体键。运行 `wev` 并按下你的键以检查确切的键名。

#### 亮度

需要：`brightnessctl`

```ini
bind=NONE,XF86MonBrightnessUp,spawn,brightnessctl s +2%
bind=SHIFT,XF86MonBrightnessUp,spawn,brightnessctl s 100%
bind=NONE,XF86MonBrightnessDown,spawn,brightnessctl s 2%-
bind=SHIFT,XF86MonBrightnessDown,spawn,brightnessctl s 1%
```

#### 音量

需要：`wpctl` (WirePlumber)

```ini
bind=NONE,XF86AudioRaiseVolume,spawn,wpctl set-volume @DEFAULT_SINK@ 5%+
bind=NONE,XF86AudioLowerVolume,spawn,wpctl set-volume @DEFAULT_SINK@ 5%-
bind=NONE,XF86AudioMute,spawn,wpctl set-mute @DEFAULT_SINK@ toggle
bind=SHIFT,XF86AudioMute,spawn,wpctl set-mute @DEFAULT_SOURCE@ toggle
```

### 浮动窗口移动

| 命令             | 参数                 | 描述                         |
| :--------------- | :------------------- | :--------------------------- |
| `smartmovewin`   | `left/right/up/down` | 按捕捉距离移动浮动窗口。     |
| `smartresizewin` | `left/right/up/down` | 按捕捉距离调整浮动窗口大小。 |
| `movewin`        | `(x,y)`              | 移动浮动窗口。               |
| `resizewin`      | `(width,height)`     | 调整窗口大小。               |
